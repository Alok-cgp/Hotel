{
  "name": "hotel-monorepo",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build && cp -r client/dist ./"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "craco": "^0.0.3",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.11"
  }
}
